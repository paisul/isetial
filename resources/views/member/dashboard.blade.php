@extends('layouts.app') @section('title','Dashboard Saya') @section('content')<div class="mx-auto max-w-6xl px-4 py-14"><div class="flex justify-between"><div><p class="text-emerald-700">Portal Anggota</p><h1 class="text-4xl font-black">Assalamu’alaikum, {{auth()->user()->name}}</h1></div>@if(auth()->user()->roles()->whereIn('slug',['super-admin','admin-masjid','ketua','bendahara','pengurus'])->exists())<a href="{{route('admin.dashboard')}}" class="h-fit rounded-lg bg-emerald-800 px-4 py-2 text-white">Admin</a>@endif</div>@if($person)<div class="mt-8 grid gap-5 md:grid-cols-3"><div class="rounded-xl bg-white p-6 shadow"><small>Masjid Asal</small><h2 class="text-xl font-bold">{{$person->membership?->homeMasjid?->name ?? 'Belum ditetapkan'}}</h2></div><div class="rounded-xl bg-white p-6 shadow"><small>Status Keanggotaan</small><h2 class="text-xl font-bold">{{$person->membership?->is_active ? 'Aktif' : 'Belum aktif'}}</h2></div><div class="rounded-xl bg-white p-6 shadow"><small>Nomor Anggota</small><h2 class="text-xl font-bold">{{$person->membership?->member_number ?? '-'}}</h2></div></div><h2 class="mt-10 text-2xl font-black">Jabatan & Divisi</h2><div class="mt-4 grid gap-3">@forelse($person->positions as $p)<div class="rounded-lg bg-white p-4">{{$p->position->name}} @if($p->division) · {{$p->division->name}}@endif</div>@empty<p>Belum ada penempatan.</p>@endforelse</div><h2 class="mt-10 text-2xl font-black">Tugas Saya</h2>@forelse($tasks as $t)<div class="mt-3 rounded-lg bg-white p-4"><b>{{$t->title}}</b> · {{$t->status}}</div>@empty<p class="mt-3">Belum ada tugas.</p>@endforelse @else<p class="mt-8">Akun belum terhubung ke data person. Hubungi admin.</p>@endif</div>@endsection
